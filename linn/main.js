// 1 способ

const regex =/шаблон/gi

//2 способ

let result = text.match(regex);
console.log(result);

const regexTwo = new RegExp('шаблон',gi)



const  text ='Привет меня зовут строка, но вы должны писать Строка';

const regex =/строка/ gi

const result  = text.replace(regex, )




const number = ' +996 505 55 55 55'
const regex = /\w/gi

const result = number.replase(regex,'')

console.log(number);
console.log(result);
constphoneNumber = document.getElementById('numberPhone');
const textPhoneNumber = phoneNumber. textContent;
const resultNumber =textPhoneNumber.replace()
console.log(resultNumber);



const user ={
   name: "Ruslan",
   surname:"Kadyrov
}

const regexUser = /[a-z/g]
console.log(${user.name.replase(regehUser'*')})   
${user.surname.replase(regexUser,'*')})

// 


 